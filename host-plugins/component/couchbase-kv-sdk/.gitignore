# The repository root ignores `.wash/`, but this project's build configuration
# lives there: `.wash/config.yaml` carries the WIT source overrides (including
# the shared interface) and the build command.
!.wash/
.wash/*
!.wash/config.yaml

# Fetched by `wash wit fetch`; regenerated by `wash build`.
wit/deps/
